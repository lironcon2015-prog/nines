# הנחיות לעבודה על המאגר הזה

## פרסום

**בסוף כל סשן, מזג את ענף העבודה ל-`main` ודחוף** — אלא אם ניתנה הוראה אחרת
במפורש. GitHub Pages מגיש את `main`, ולכן עבודה שנשארת על ענף אינה מגיעה
לטלפון: האפליקציה תמשיך להציג את הגרסה הקודמת כאילו כלום לא קרה.

```
git checkout main
git merge --no-ff <ענף העבודה>
git push -u origin main
```

## לפני כל פרסום

**הרץ `sh tools-check.sh`.** הוא משווה את הגרסאות מול מה שכבר פורסם ב-`origin/main`
ומוודא שכל קובץ JS נמצא ב-`ASSETS`. שלוש הבדיקות האלה נכשלות בשקט בלעדיו — וזה
כבר קרה: העלאת גרסה שלא תפסה, שהוצגה כאילו הצליחה כי הודפסה ממחרוזת קבועה
במקום מהקובץ. **אל תדווח על מספר גרסה בלי לקרוא אותו מהקובץ.**

- **העלה את `VERSION` ב-`sw.js`** (`v28` → `v29`). הקוד מוגש cache-first מתוך
  מטמון שנושא את מספר הגרסה; בלי העלאה, המכשיר ימשיך להגיש את הקוד הישן גם
  אחרי שהפריסה הצליחה.
- **העלה את `version` ב-`content/booklets.json`.** הוא מוצג בתחתית מסך הבית,
  וזו הדרך היחידה לדעת במבט אם העדכון הגיע למכשיר.
- **קובץ JS חדש חייב להיכנס ל-`ASSETS` ב-`sw.js`**, אחרת הוא לא יישמר למצב
  לא-מקוון.

תוכן (`content/*.json`) נשלף מהרשת קודם ואינו דורש העלאת גרסה.

## קלט שמגיע מהדבקה

**לחלץ, לא לבדוק.** כתובת או קוד שמודבקים מטלפון מגיעים עם סימני כיווניות,
שורות שנשברו ורווחים — ובדיקה מעוגנת נכשלת על תו שאי אפשר לראות, ואומרת
למשתמש שהערך שלו שגוי כשהוא תקין. חפש את הצורה הידועה **בתוך** מה שהתקבל.

**הודעת שגיאה חייבת להראות את מה שהתקבל, בשלמותו ובלי חיתוך**, עם תווים בלתי
נראים מוצגים כקוד. חיתוך ב-60 תווים הסתיר בדיוק את הזנב שבו הייתה הבעיה.

## חיסכון בטוקנים — פעיל בכל סשן

הכללים המלאים ב-`.claude/skills/token-efficient-workflow/SKILL.md`. הקובץ הזה
נטען אוטומטית בכל סשן, ולכן העיקר חוזר כאן:

- **לחפש לפני שקוראים.** `grep -n` ממוקד או `head`, ולא קריאת קובץ שלם. לא
  לקרוא קובץ ספקולטיבית, ולא לקרוא שוב מה שכבר בהקשר.
- **לקרוא טווח, לא קובץ.** `offset`/`limit` כשזה מספיק.
- **לערוך נקודתית.** `Edit` על הבלוק, לא כתיבה מחדש של הקובץ.
- **בלי מילות מילוי.** בלי "אני עכשיו…", בלי "עדכנתי את…", בלי סיכום של מה
  שהדיף כבר מראה. להתחיל מהתשובה.
- **תכנון בשקט.** תיקון פשוט מבוצע ישירות. תוכנית לא מודפסת.
- **שאלות באצווה.** כל ההבהרות בהודעה אחת.

**שלושה דברים גוברים על "שתיקה עדיפה", וכל אחד מהם כבר נכשל כאן בפועל:**

1. **מספר גרסה נקרא מהקובץ.** `sh tools-check.sh` לפני פרסום. אף פעם לא לדווח
   גרסה ממחרוזת שנכתבה ביד — זה בדיוק מה שהסתיר פרסום שלא תפס.
2. **מיזוג ל-`main` בסוף סשן**, ולומר שזה נעשה. עבודה שנשארת על ענף לא מגיעה
   לטלפון.
3. **כישלון מדווח.** בדיקה שנפלה, שלב שדולג, או הנחה שהונחה — נאמרים במפורש.
   הקיצור חל על נימוסים ועל תיאור מה שהדיף מראה, לא על תוצאות.

## סגנון

הערות בעברית, ומסבירות **למה** ולא מה. הרבה מההערות בקוד הזה מתעדות מלכודת
שנפלנו לתוכה — עדיף להוסיף לתיעוד הזה מאשר לחזור עליה.

בלי תלויות ובלי שלב בנייה: HTML, CSS ו-ES modules בלבד.

## בדיקה

`python3 -m http.server` בשורש, ואז הדפדפן. יש Chromium ו-Playwright מותקנים
בסביבה, ושווה להריץ מעבר על כל המסכים לפני פרסום — `#/`, `#/b/<חוברת>`,
`#/t/<חוברת>`, `#/q/<חוברת>`, `#/v`, `#/s`.
